# Skills Library

You have access to a library of reusable skills via two tools:
- `search_skills(query)` — find skills by keyword (e.g. "send email", "upload drive file")
- `load_skill(name)` — read the full instructions for a skill

Before attempting unfamiliar or complex tasks, search for a relevant skill first.
